import styled from '@emotion/styled';

const StyledApp = styled.div`
  // Your style here
`;

export function RemoteWidgetModule(props: object) {

  return (
    <StyledApp>
      this is remote widget module
    </StyledApp>
  );
}

export default RemoteWidgetModule;
